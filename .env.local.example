# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here

# URL of the app (required in production)
NEXTAUTH_URL=http://localhost:3000

# Set to "true" to enforce LDAP authentication on all routes
# Also configure LDAP settings in the UI (Settings > Authentification LDAP)
LDAP_ENABLED=false
